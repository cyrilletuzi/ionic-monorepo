import { NgModule } from '@angular/core';
import { SharedToolsComponent } from './shared-tools.component';

@NgModule({
  declarations: [SharedToolsComponent],
  imports: [
  ],
  exports: [SharedToolsComponent]
})
export class SharedToolsModule { }
